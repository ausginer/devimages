# Codex CLI

Installs the latest `@openai/codex` package globally with npm.

## Usage

```json
"./features/codex-cli": {}
```

The feature requires npm and declares that it should install after the Dev Container Node.js feature.
