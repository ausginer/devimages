# Dura

Installs the latest locked release of the Dura background Git commit tool into `/usr/local` with Cargo.

## Usage

```json
"./features/dura": {}
```

The feature requires Cargo and an apt-based image. It installs the native compiler, pkg-config, and OpenSSL development packages needed for the Cargo build.
