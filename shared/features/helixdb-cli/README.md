# HelixDB CLI

Installs the HelixDB CLI system-wide with the project's official installer.

## Usage

```json
"./features/helixdb-cli": {}
```

The feature requires `curl`. It is available to image definitions in this repository but is not currently included in a published image.
