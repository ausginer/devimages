# Just

Installs the latest locked release of the Just command runner into `/usr/local` with Cargo.

## Usage

```json
"./features/just": {}
```

The feature requires Cargo and an apt-based image. It installs the native compiler and pkg-config needed for the Cargo build.
