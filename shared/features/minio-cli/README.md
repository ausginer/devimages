# MinIO Client

Installs the official Linux AMD64 `mc` binary as `/usr/local/bin/mc`.

## Usage

```json
"./features/minio-cli": {}
```

The feature requires `curl` and currently supports AMD64 images. It is available to image definitions in this repository but is not currently included in a published image.
