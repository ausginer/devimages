{
  "id": "opencode-cli",
  "version": "1.0.0",
  "name": "OpenCode CLI",
  "description": "Installs the OpenCode CLI via the official curl installer.",
  "options": {
    "username": {
      "type": "string",
      "default": "node",
      "description": "User to install OpenCode CLI for."
    }
  },
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
